---
import convertMarkdown from '@src/utils/convertMarkdown';

type Props = {
  markdown: string;
};

const { markdown } = Astro.props;

const convertedHtml = await convertMarkdown(markdown);
---

<div
  class="prose prose-xl mx-auto max-w-full prose-pre:rounded-lg prose-pre:bg-transparent prose-pre:p-0 prose-img:mx-auto prose-img:rounded-lg"
  set:html={convertedHtml}
/>
