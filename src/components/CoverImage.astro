---
import { Image } from 'astro:assets';

type Props = {
  imageUrl: string | undefined;
  altText: string;
};

const { imageUrl, altText } = Astro.props;
---

{
  imageUrl ? (
    <Image src={imageUrl} height="420" width="800" alt={altText} />
  ) : (
    <div class="aspect-[800/420] w-full bg-gradient-to-r from-green-200 to-blue-200" />
  )
}
